document.addEventListener("DOMContentLoaded",()=>{const form=document.getElementById("formCita");if(!form)return;const garantiaTexto=document.getElementById("garantiaTexto");if(garantiaTexto&&CONFIG.garantiaImporte){garantiaTexto.textContent=CONFIG.garantiaImporte;}
const campoFecha=form.querySelector("#citaFecha");const ahora=new Date();ahora.setMinutes(ahora.getMinutes()-ahora.getTimezoneOffset());campoFecha.min=ahora.toISOString().slice(0,16);form.addEventListener("submit",(e)=>{e.preventDefault();const nombre=form.querySelector("#citaNombre").value.trim();const valor=campoFecha.value;const motivo=form.querySelector("#citaMotivo").value.trim();const acepta=form.querySelector("#citaAcepta");const aviso=document.getElementById("citaAviso");const modalidad=(form.querySelector('input[name="modalidad"]:checked')||{}).value||"Presencial";if(!valor)return;if(acepta&&!acepta.checked){aviso.textContent="Para pedir la cita tienes que aceptar las condiciones de reserva (la casilla de arriba).";acepta.focus();return;}
const fecha=new Date(valor);const fechaBonita=fecha.toLocaleDateString("es-ES",{weekday:"long",day:"numeric",month:"long",year:"numeric"});const horaBonita=fecha.toLocaleTimeString("es-ES",{hour:"2-digit",minute:"2-digit"});const cuerpo="Hola,\n\n"+"Me gustaría pedir una cita:\n\n"+"· Día: "+fechaBonita+"\n"+"· Hora: "+horaBonita+"\n"+"· Modalidad: "+modalidad+"\n"+
(motivo?"· Motivo: "+motivo+"\n":"")+"\nHe leído y acepto las condiciones de reserva: la garantía de "+
(CONFIG.garantiaImporte||"5€")+" por Bizum, que se descuenta del "+"precio de la sesión y no se devuelve si no acudo sin avisar con 24 horas.\n\n"+"Quedo a la espera de tu confirmación.\n\n"+"— "+nombre+(function(){try{const inf=localStorage.getItem("informeTest");return inf?"\n\n\n"+inf+"\n\n(El informe del test se adjunta automáticamente porque hice el test de orientación en la web.)":"";}catch(e){return"";}})();const destino="mailto:"+CONFIG.email+"?subject="+encodeURIComponent("Solicitud de cita "+modalidad.toLowerCase()+" — "+nombre+" — "+fechaBonita+" "+horaBonita)+"&body="+encodeURIComponent(cuerpo);window.location.href=destino;aviso.textContent="Se ha abierto tu correo con la solicitud lista. "+"Dale a enviar y la psicóloga te confirmará el hueco "+"(y te indicará cómo pagar la garantía). Nada se ha guardado en esta web.";});});
